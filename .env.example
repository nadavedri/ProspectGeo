# PostgreSQL configuration (Example values)
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB=mydb

# App-specific configuration (Example values)
DATABASE_URL=postgresql://user:password@db:5432/mydb